[**tinky-theme**](../README.md)

---

[tinky-theme](../globals.md) / defaultTheme

# Variable: defaultTheme

> `const` **defaultTheme**: [`Theme`](../interfaces/Theme.md)

The standard default instance of a [Theme](../interfaces/Theme.md).

## Remarks

This constant provides an initial empty state for the theme context.
It is primarily used as a fallback when no `ThemeProvider` is present in the component tree.
